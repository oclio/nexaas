---
'saaskip': minor
---

Centralize day-one customization files into `src/config/` and add onboarding guide. Rename `app` export to `brand` and `{app}` i18n placeholder to `{brand}`. Move navigation and icons registry into `src/config/`. Drop redundant `app.keywords` and inline `app.logo`.
