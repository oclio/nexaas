---
'nexaas': minor
---

Added internationalization (i18n) support with next-intl: locale-prefixed routing (`/en`, `/fr`), type-safe messages with `TranslationSchema`, `LocaleSwitcher` component, and `withIntl` middleware for API locale header resolution.
